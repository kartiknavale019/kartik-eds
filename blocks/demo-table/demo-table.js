export default function decorate(block) {
  // We leave this empty! 
  // By not destroying the default <div> elements AEM creates, 
  // the Universal Editor can successfully track and display our rows.
}